name: "Protocol Buffer Go Stubs Generation"
on:
  push:
    branches:
      - '*'
jobs:
  protoc:
    name: "Generate"
    runs-on: ubuntu-latest
    steps:
      - name: Install Go
        uses: actions/setup-go@v5
        with:
          go-version: 1.22
      - uses: actions/checkout@v4
      - name: "Generate for Golang"
        run: |
          sudo apt-get install -y protobuf-compiler golang-goprotobuf-dev
          go install google.golang.org/protobuf/cmd/protoc-gen-go@latest
          go install google.golang.org/grpc/cmd/protoc-gen-go-grpc@latest
          mkdir -p golang
          protoc --go_out=./golang --go_opt=paths=source_relative --go-grpc_out=./golang --go-grpc_opt=paths=source_relative ./**/*.proto
          cd golang
          go mod init github.com/OkabeRintarouBeta/microservice-protobuf
          go mod tidy
          git config --global user.email "zihuiliu@umich.edu"
          git config --global user.name "OkabeRintarouBeta"
          git add .
          git commit -am "proto update"
          git push origin main